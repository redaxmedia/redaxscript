<?php namespace Redaxscript\Admin\Template; ?>

<?php if (Tag::registry('loggedIn') === Tag::registry('token') && Tag::registry('firstParameter') !== 'logout'): ?>
	<?php Tag::link()
		->init()
		->appendFile('//fonts.googleapis.com/css?family=Open+Sans+Condensed:300,700&amp;subset=' . Helper::getSubset())
		->appendFile('templates/admin/dist/styles/admin.min.css');
	?>
<?php endif; ?>

<!--@todo: do not echo the tags here, we just extend the collection !-->
<!--@todo add a Tag::script after the Tag::link block and load all the scripts from .loader - delete the file after porting !-->