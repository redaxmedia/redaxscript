<?php
namespace Redaxscript;

$preElement = new Html\Element();
$preElement->init('pre',
[
	'class' => $admin ? 'rs-admin-js-code rs-admin-code-default' : 'rs-js-code rs-box-blockcode'
]);

echo $preElement->html(
'if ($blockcode)
{
	return true;
}');