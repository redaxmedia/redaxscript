<?php
namespace Redaxscript;

$preElement = new Html\Element();
$preElement->init('pre',
[
	'class' => $admin ? 'rs-admin-js-blockcode rs-admin-box-blockcode' : 'rs-js-blockcode rs-box-blockcode'
]);

echo $preElement->html(
'if ($blockcode)
{
	return true;
}');