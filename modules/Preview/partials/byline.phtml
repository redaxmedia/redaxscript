<?php
namespace Redaxscript;

$registry = Registry::getInstance();
$byline = new View\Helper\Byline(Registry::getInstance(), Language::getInstance());
$byline->init();
echo $byline->render($registry->get('now'), 'admin');
