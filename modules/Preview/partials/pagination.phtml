<?php
namespace Redaxscript;

$pagination = new View\Helper\Pagination(Registry::getInstance(), Language::getInstance());
$pagination->init();
echo $pagination->render('preview/pagination', 5000, 10000);
echo $pagination->render('preview/pagination', 5000, 10000, 1);

for ($i = 1; $i < 11; $i++)
{
	echo $pagination->render('preview/pagination', $i, 10, 2);
}