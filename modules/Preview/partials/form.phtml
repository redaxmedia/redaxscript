<?php
namespace Redaxscript;

$registry = Registry::getInstance();
$language = Language::getInstance();
$id = uniqid($admin ? 'admin-form-' : 'form-');
$i = 0;
$formElement = $admin ? new Admin\Html\Form($registry, $language) : new Html\Form($registry, $language);
$formElement
	->init()
	->append('<fieldset><ul><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->text(
	[
		'id' => $id . $i,
		'placeholder' => 'Placeholder',
		'required' => 'required'
	])
	->append('</li><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->text(
	[
		'disabled' => 'disabled',
		'id' => $id . $i,
		'placeholder' => 'Placeholder'
	])
	->append('</li><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->selectRange(
	[
		'min' => 1,
		'max' => 10
	],
	[
		'id' => $id . $i,
		'required' => 'required'
	])
	->append('</li><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->selectRange(
	[
		'min' => 1,
		'max' => 10
	],
	[
		'disabled' => 'disabled',
		'id' => $id . $i
	])
	->append('</li><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->textarea(
	[
		'class' => $admin ? 'rs-admin-field-textarea rs-admin-field-small' : 'rs-field-textarea rs-field-small',
		'id' => $id . $i
	])
	->append('</li><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->textarea(
	[
		'class' => $admin ? 'rs-admin-field-textarea rs-admin-field-small' : 'rs-field-textarea rs-field-small',
		'disabled' => 'disabled',
		'id' => $id . $i
	])
	->append('</li><li>')
	->label($language->get(++$i, '_number'),
	[
		'for' => $id . $i
	])
	->textarea(
	[
		'id' => $id . $i,
		'required' => 'required'
	])
	->append('</li></ul></fieldset>')
	->submit()
	->reset();
?>

<!-- form -->

<?php echo $formElement; ?>